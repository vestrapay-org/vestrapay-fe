import { Callout } from "@/components/docs/callout";
import { CodeTabs } from "@/components/docs/code-tabs";
import { CodeBlock } from "@/components/docs/code-block";

export const metadata = {
  title: "Quick Start",
  description: "Make your first Vestrapay API call in under five minutes.",
};

<div className="mb-12 max-w-3xl">
  <h1 className="mb-4 text-3xl font-bold tracking-tight">Quick Start</h1>
  <div className="mb-10 text-lg text-muted-foreground">
    This guide gets you from zero to a working payment in under five minutes. You only need an API key.
  </div>
</div>

<div className="space-y-24">

{/* ── GET KEY ── */}
<section className="grid items-start gap-12 lg:grid-cols-2">
  <div>
    <h2 id="get-key" className="mt-0 pt-0 text-xl font-semibold border-none">1. Get your API key</h2>
    <div className="text-[15px] text-muted-foreground mb-6">
      Sign in to the <a href="https://vestrapay.com" target="_blank" rel="noreferrer">Vestrapay Dashboard</a> and navigate to **Settings → API Keys**.
      Copy your test secret key. It starts with `sk_test_`.
    </div>

    <Callout type="warning" title="Keep your secret key private">
      Never expose your secret key in client-side code, version control, or public repositories. Only the publishable key (`pk_test_...`) is safe to use in the browser.
    </Callout>
  </div>
  <div className="sticky top-24 lg:mt-0 mt-8 space-y-4">
    <CodeBlock code={'sk_test_51Mz0q...'} lang="text" title="Example Test Secret Key" />
  </div>
</section>

<div className="h-px bg-border my-12" />

{/* ── CREATE SEC ── */}
<section className="grid items-start gap-12 lg:grid-cols-2">
  <div>
    <h2 id="create-payment" className="mt-0 pt-0 text-xl font-semibold border-none">2. Create a payment</h2>
    <div className="text-[15px] text-muted-foreground mb-6">
      Send a `POST` request to `/v2/payments`. The only required fields are `amount` and `currency`.
    </div>
  </div>
  <div className="sticky top-24 lg:mt-0 mt-8 space-y-4">
    <CodeTabs tabs={[
      {
        label: "cURL",
        lang: "bash",
        code: `curl -X POST "https://api.vestrapay.com/v2/payments" \\
  -H "Authorization: Bearer sk_test_your_key" \\
  -H "Content-Type: application/json" \\
  -d '{
    "amount": 5000,
    "currency": "NGN",
    "description": "Order #1042"
  }'`
      },
      {
        label: "Node.js",
        lang: "javascript",
        code: `import Vestrapay from "@vestrapay/node";

const vestrapay = new Vestrapay("sk_test_your_key");

const payment = await vestrapay.payments.create({
  amount: 5000,
  currency: "NGN",
  description: "Order #1042",
});

console.log(payment.checkout_url);`
      },
      {
        label: "Python",
        lang: "python",
        code: `import vestrapay

vestrapay.api_key = "sk_test_your_key"

payment = vestrapay.Payment.create(
    amount=5000,
    currency="NGN",
    description="Order #1042",
)

print(payment.checkout_url)`
      }
    ]} />
    <CodeBlock code={`{
  "id": "pay_k9mPlx3Rw7QzA4",
  "object": "payment",
  "amount": 5000,
  "currency": "NGN",
  "status": "pending",
  "description": "Order #1042",
  "checkout_url": "https://checkout.vestrapay.com/pay_k9...",
  "created": 1745002345
}`} lang="json" title="Response (201 Created)" />
  </div>
</section>

<div className="h-px bg-border my-12" />

{/* ── REDIRECT ── */}
<section className="grid items-start gap-12 lg:grid-cols-2">
  <div>
    <h2 id="redirect" className="mt-0 pt-0 text-xl font-semibold border-none">3. Redirect the customer</h2>
    <div className="text-[15px] text-muted-foreground mb-6">
      The `checkout_url` is where you send your customer to complete payment. The payment starts as `pending` and transitions to `succeeded` or `failed` after the customer acts.
    </div>
  </div>
  <div className="sticky top-24 lg:mt-0 mt-8 space-y-4">
    <CodeTabs tabs={[
      {
        label: "Browser",
        lang: "javascript",
        code: `// After creating the payment server-side, redirect:
window.location.href = payment.checkout_url;`
      },
      {
        label: "Express.js",
        lang: "javascript",
        code: `app.post("/checkout", async (req, res) => {
  const payment = await vestrapay.payments.create({
    amount: req.body.amount,
    currency: "NGN",
  });
  res.redirect(302, payment.checkout_url);
});`
      }
    ]} />
  </div>
</section>

<div className="h-px bg-border my-12" />

{/* ── WEBHOOK ── */}
<section className="grid items-start gap-12 lg:grid-cols-2">
  <div>
    <h2 id="listen" className="mt-0 pt-0 text-xl font-semibold border-none">4. Listen for the result</h2>
    <div className="text-[15px] text-muted-foreground mb-6">
      Once the customer pays, Vestrapay fires a `payment.succeeded` webhook. Configure your endpoint in **Dashboard → Webhooks**.
    </div>
    <Callout type="success" title="You're done">
      You have successfully created a payment and set up a webhook handler. Everything else in the documentation builds on these foundations.
    </Callout>
  </div>
  <div className="sticky top-24 lg:mt-0 mt-8 space-y-4">
    <CodeTabs tabs={[
      {
        label: "Express.js",
        lang: "javascript",
        code: `app.post("/webhooks", express.raw({ type: "application/json" }), (req, res) => {
  const sig = req.headers["x-vestrapay-signature"];
  const event = vestrapay.webhooks.constructEvent(req.body, sig, process.env.WEBHOOK_SECRET);

  if (event.type === "payment.succeeded") {
    await fulfillOrder(event.data.id);
  }

  res.status(200).json({ received: true });
});`
      }
    ]} />
  </div>
</section>

<div className="h-px bg-border my-12" />

{/* ── TEST CARDS ── */}
<section className="grid items-start gap-12 lg:grid-cols-2">
  <div>
    <h2 id="test-cards" className="mt-0 pt-0 text-xl font-semibold border-none">Test cards</h2>
    <div className="text-[15px] text-muted-foreground mb-6">
      Use these card numbers in the test environment. No real charges are made.
    </div>
  </div>
  <div className="sticky top-24 lg:mt-0 mt-8 space-y-4">
    <table>
      <thead>
        <tr>
          <th>Scenario</th>
          <th>Card number</th>
          <th>CVV</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>Successful payment</td>
          <td><code>4084 0841 7711 1111</code></td>
          <td>Any</td>
        </tr>
        <tr>
          <td>Declined (funds)</td>
          <td><code>4084 0841 7711 1114</code></td>
          <td>Any</td>
        </tr>
        <tr>
          <td>Declined (invalid)</td>
          <td><code>4084 0841 7711 1115</code></td>
          <td>Any</td>
        </tr>
        <tr>
          <td>3D Secure req</td>
          <td><code>4084 0841 7711 1116</code></td>
          <td>Any</td>
        </tr>
      </tbody>
    </table>
  </div>
</section>

</div>
